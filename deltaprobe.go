package main

// deltaprobe exercises the staging delta path end to end: a real flag key
// the scan can file a fact for. The file arrives and leaves in probe commits.
const deltaProbeFlagKey = "ac-bulk-update-access-check"
